---
title: "R Notebook"
output: html_notebook
---

```{r}
arran2016 %>%
  ggplot() +
  geom_sf() +
  theme_grey() +
  theme(legend.position="none") +
  theme(axis.text.x=element_blank(),
        axis.ticks.x=element_blank())

```

  geom_sf(data=simple.sf)
  
  
  
```{r}
arransubsect %>%
  ggplot() +
  geom_sf() +
  theme_grey() +
  theme(axis.text.x=element_blank(),
        axis.ticks.x=element_blank()) +
  geom_sf(data=simple.sf, shape=20)
  
```

```{r}
longitude <- arrancoordinates[,"longitude"]
latitude <- arrancoordinates[,"latitude"]
```

annotate("point", x = -80, y = 35, colour = "red", size = 4)

```{r}
world <- map_data("world")
world2 <- filter(world, subregion == 'Scotland')
```

```{r}
ggplot(world2, aes(long, lat, group = group)) +
  geom_polygon(fill = "white", colour = "black")
```

